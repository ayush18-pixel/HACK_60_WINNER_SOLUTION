# Local Build Artifacts

This folder is for generated bundles, smoke-test outputs, staged deploy assets,
and zip files produced during local development. It is intentionally excluded
from GitHub pushes.

Examples include:

- cloud runtime bundles from `scripts/prepare_cloud_assets.py`
- training bundles and zip exports
- smoke-test checkpoints and evaluation outputs
